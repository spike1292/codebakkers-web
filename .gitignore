node_modules
dist
.astro
.turbo
.DS_Store
*.log
.env
.env.*
!.env.example
!**/.env.example
.wrangler

# local planning + weekly outputs — never publish
PLAN.md
weekly/

# personal source docs — never publish/commit
private/

# generated search index (mirrored from dist for dev)
apps/*/public/pagefind/
