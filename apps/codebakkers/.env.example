# Cloudflare Web Analytics beacon token for this site (cookieless).
# Dashboard → Analytics & Logs → Web Analytics → this site → beacon token.
# Optional: unset means no analytics script is emitted.
# In CI this is injected from the GitHub secret CF_BEACON_TOKEN_CODEBAKKERS.
PUBLIC_CF_BEACON_TOKEN=
